# Runtime dependencies for the Catch-22 reproduction pipeline (common/ +
# Llama2-Watermark/). The upstream method repositories (SemStamp, PMark,
# HeavyWater/SimplexWater) are cloned by scripts/fetch_external.sh and carry
# their own requirements.txt; install those into the same environment.
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
bitsandbytes>=0.41.0            # 4-bit weight loading (load_in_4bit)
huggingface-hub>=0.19.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0            # AUROC / ROC used by common/io_utils.py
pandas>=2.0.0
tqdm>=4.65.0
sentencepiece>=0.1.99
protobuf>=3.20.0
jsonlines>=3.1.0
pyyaml>=6.0
nltk>=3.8                      # synonym / sentence tokenization for attacks
sentence-transformers>=2.2.0  # semantic embedders (SimMark / PMark)
sacremoses>=0.0.53            # Marian back-translation tokenization
editdistance>=0.6             # token edit-rate estimation

# Optional: utility/quality metrics (MAUVE, BERTScore). Install with:
#   pip install mauve-text bert-score
